<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:PrimeiroApp.ViewModel"
             x:Class="PrimeiroApp.View.Cabecalho">
    <ContentView.BindingContext>
        <vm:UsuarioViewModel />
    </ContentView.BindingContext>
    
      <ContentView.Content>
          <StackLayout>
            <Label Text="Seja bem vindo!" HorizontalOptions="Center"/>
            <Label Text="" x:Name="lblUsuario" HorizontalOptions="Center"/>
            <Button Text="Logout" Clicked="Button_Clicked"></Button>
          </StackLayout>
      </ContentView.Content>
</ContentView>