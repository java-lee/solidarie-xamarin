<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:PrimeiroApp.ViewModel"
             x:Class="PrimeiroApp.View.AlterarExcluirAnimal"
             Title="Detalhe do Animal">
    <ContentPage.BindingContext>
        <vm:AnimalViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <Grid VerticalOptions="StartAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <StackLayout Grid.Row="0">
                <Label Text="Nome do Animal:"></Label>
                <Entry Placeholder="Informe o nome do animal" Text="{Binding Nome}" x:Name="Email"></Entry>
                <Button Text="Alterar" Command="{Binding BotaoAlterar}"></Button>
                <Button Text="Excluir" Command="{Binding BotaoExcluir}"></Button>
            </StackLayout>

        </Grid>

    </ContentPage.Content>
</ContentPage>